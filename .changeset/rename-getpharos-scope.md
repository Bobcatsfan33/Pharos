---
"@getpharos/sdk": patch
"@getpharos/middleware": patch
"@getpharos/pdp-spec": patch
---

Rename publishable packages to the `@getpharos` scope and the Python distribution to `getpharos` (the `pharos` npm scope and PyPI names were taken). Import paths change accordingly; the Python module name `pharos_sdk` is unchanged.
