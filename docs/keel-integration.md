# Keel integration has moved

Keel now ships inside this repository as **Pharos Runtime**. See the canonical
[Pharos Runtime documentation](runtime.md).

The `keel` command and Python implementation namespace remain compatibility aliases for
existing integrations.
