node_modules
**/dist
**/.next
**/coverage
**/.venv
runtime/python
pnpm-lock.yaml
LICENSE
NOTICE
.pharos-keystore
*.md
# Committed, generated eval datasets — content-hashed, not hand-formatted (see judge-eval).
packages/judge-eval/data
# Python training pipeline (venv, model blobs, generated corpus/lockbox JSON).
training
# Helm templates contain Go template directives that Prettier's YAML parser cannot parse.
deploy/helm/templates
