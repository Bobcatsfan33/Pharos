{
  "name": "@pharos/gateway",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx --env-file=../../.env src/server.ts"
  },
  "dependencies": {
    "@pharos/sdk": "workspace:*",
    "fastify": "^5.10.0"
  }
}
